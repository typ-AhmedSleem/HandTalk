package com.typ.handtalk.impl.launcher

import com.typ.handtalk.impl.HandSignRecognizer

actual fun launchHandTalkRecognizer(recognizer: HandSignRecognizer) {
    // iOS implementation (N/A for now)
}
