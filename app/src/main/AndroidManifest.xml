<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    android:installLocation="preferExternal">

    <!-- Enable instant app support -->
    <dist:module dist:instant="true" />

    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.HandTalk.HomeActivity"
        tools:targetApi="31">

        <activity
            android:name=".ui.activities.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activities.LiveSignTranslatorActivity"
            android:theme="@style/Theme.HandTalk"
            android:exported="true" />

        <activity
            android:name=".ui.activities.HomeActivity"
            android:exported="true" />

        <activity
            android:name=".ui.activities.Arabic2SignTranslatorActivity"
            android:exported="true" />

    </application>

</manifest>